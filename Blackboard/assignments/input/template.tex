
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%PANDOC SPECIFIC SHIT, TAKEN FROM ANOTHER TEMPLATE...

\documentclass[$if(fontsize)$$fontsize$,$endif$$if(lang)$$babel-lang$,$endif$$if(papersize)$$papersize$paper,$endif$$for(classoption)$$classoption$$sep$,$endfor$]{$documentclass$}

%Deal with margins and other geometry stuff
\usepackage[$if(geometry_opts)$$geometry_opts$, $endif$]{geometry}

%Double-spacing or whatever...
$if(linestretch)$
\usepackage{setspace}
\setstretch{$linestretch$}
$endif$

%Some of this is math package stuff, but honestly i don't really get
%what most of it is doing
\usepackage{amssymb,amsmath}
\usepackage{ifxetex,ifluatex}
\usepackage{fixltx2e} % provides \textsubscript

%Numbered section spacing
$if(numbersections)$
\setcounter{secnumdepth}{2}
$else$
\setcounter{secnumdepth}{0}
$endif$

%For LIST spacing
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% LUCY'S DOCUMENT PREAMBLE AND PACKAGES

\widowpenalty10000
\clubpenalty10000

\usepackage{pdflscape}
\usepackage{xcolor}
\definecolor{light-gray}{gray}{0.95}

\usepackage{tcolorbox}
\newtcolorbox{blackbox}{
  colback=light-gray,
  colframe=black,
  coltext=black,
  boxsep=2pt,
  arc=4pt}


%Set paragraph indent and between paragraph spacing
\setlength\parindent{0pt}
\setlength{\parskip}{8pt}

%Deal with titles and make them less stupid and ugly
\usepackage{titlesec}
\titleformat{\section}[block]{\bfseries\sc\filcenter}{}{1em}{}
\titleformat{\subsection}[hang]{\bfseries}{}{1em}{}
\setcounter{secnumdepth}{0}

\usepackage[hyphens]{url}

%Bunch of hyperlink shit
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=violet,
    filecolor=magenta,      
    urlcolor=cyan,
    citecolor = black
}

%Need all these for graphics and tables
\usepackage{subfig}
\usepackage{graphicx}
\usepackage{blindtext}
\usepackage{array}
\usepackage{float}

%Header and footer junk
\usepackage{lastpage}
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhead[L]{$title$}
\fancyhead[C]{}
\fancyhead[R]{\small{\href{http://ledelaney.org/cb-tutorials/blackboard/BIOS-Bb-Assignments.html}{click here for web version}}}
\fancyfoot[L]{\textsc{$subtitle$}}
    \fancyfoot[R]{\tiny{$version$: \today\ | $author$}}
\fancyfoot[C]{\thepage\ of \pageref{LastPage}}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% START OF THE DOCUMENT BODY
\begin{document}

\tableofcontents
\newpage

%%%%%%%%%%%%%% DOCUMENT BODY
$body$

\end{document}
